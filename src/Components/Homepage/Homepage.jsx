import { Box } from "@mui/material";

function Homepage() {
  return (
    <Box
      sx={{
        width: "100%",
        height: "100%",
        backgroundColor: "black",
        color: "white",
      }}
    >
      this is the homepage
    </Box>
  );
}

export default Homepage;
